apiVersion: v1
kind: Service
metadata:
  name: agentic-sre-api
  namespace: agentic-sre
spec:
  type: NodePort
  selector:
    app: agentic-sre-api
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30080
