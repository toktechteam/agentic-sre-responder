apiVersion: v1
kind: Service
metadata:
  name: agentic-sre-ui
  namespace: agentic-sre
spec:
  type: NodePort
  selector:
    app: agentic-sre-ui
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30081
